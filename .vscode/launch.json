{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "mdbook serve --open",
            "name": "Start live preview server",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}